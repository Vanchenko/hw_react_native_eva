export const selectPosts = (state) => state.posts.posts;
export const selectUser = (state) => state.auth.user;
export const selectUserName = (state) => state.auth.loginName;
export const selectEmail = (state) => state.auth.user.email;
